# 验证记录

日期：2026-09-08，Windows 11 / Node 22.21.1。

## V9 / 0.9.0 原始分辨率默认导出

验证日期：2026-09-09。

- 按用户要求，单张下载、大图/聊天预览与 ZIP 默认使用 `size=original`。省略尺寸也保持原图宽高；长方形不补正方形、不降采样或放大。128/256/512/1024 仍是主动指定的兼容选项，界面默认明确为「原始分辨率（默认）」。
- 后期文字在原尺寸画布上生成字形，字号以短边同比例计算，使用真实宽高定位。原生文字、无字/未启用/空字幕路径直接返回存储 PNG 原字节；原尺寸叠字保持非文字区域 RGBA 像素。灰度与灰度 Alpha 素材经过既有保存流程验证，进入叠字阶段均为四通道，不需要额外修改。
- TypeScript/Vite 生产构建与 **91/91** 测试通过，覆盖横竖矩形、原尺寸默认值、原字节直通、原尺寸字体及文字外像素、原生字防叠加、原尺寸 ZIP 结构与显式数值尺寸兼容。原尺寸 ZIP 不包含冗余 `resized/`；配方记录导出模式及源图宽高。
- 圆形描边改为滑动最大值计算，高分辨率渲染不再按每个前景像素枚举整个圆。另进行 72 个固定种子随机/稀疏/分级掩码的独立等价检查，与原算法逐字节比较无差异；此项不计入 91 项测试。实测 4000×3000 五字圆体叠字由约 2.75 秒降到约 0.37 秒，1254 方图约 54 毫秒，字体首次加载另计。
- 实际调用省略 size 的 ZIP 接口重导出全套 76 张，输出均与其源图一致为 **1254×1254**。19 张无需后期叠字的成品与同包原图逐字节一致；其余文字在原分辨率重绘。完整包 154 项条目；高清成品包 76 张 PNG、总览和说明。尺寸/直通校验报告保存在私有 artifact 中，未把原 512 成品放大来代替原图。
- 浏览器确认默认原尺寸、可手动选 512 并切回原尺寸；实际大图请求为 `size=original` 且显示 1254×1254。手机 390×844 的尺寸下拉框、导出按钮和说明完整显示，检查后恢复默认视口，控制台 error/warn 日志为空。已目视检查原尺寸排字成品。

## V8 / 0.8.0 完整 76 张与俏皮暧昧选集

验证日期：2026-09-09。

- 用户请求补齐当前 70 个配方，并确认加入 6 张非露骨「俏皮暧昧」。新增秘密眨眼、托腮邀约、邀请靠近、藏礼物、悄悄话、被夸脸红六个可选配方，默认选集保持不变；原 `all70` 仍为 70 项，新增 `all76`。
- 原有 24 张保留，补齐 46 张并新增 6 张，共导入/校验 52 个新结果；运行 API 核对当前 76 个反应都有成功图片，缺项为 0。全部由内置 image_gen 真实生成，不是用户第三方 API 付费调用。个人原图、结果、过程记录只在忽略的 data/artifacts 中保存。
- 原 46 张初次出图有 36 张得到图，10 张在输出检查阶段未得到图。后者换成实际不同的普通脸部/道具构图，仅头手或由抱枕、饭碗、电脑构成画面，无躯干，不修改衣装；这十个新构图各一次生成成功，原错误与实际新提示词分开留档。原被拒绝结果未计入完成数。
- 额外修正「等到了还要装偶遇」的构图：人物右置、书降到下巴以下露出嘴部与小笑，保留原候选。最终版本仍有少量发梢进入留白区，未承诺精确像素布局。举星星的尖角少量触边等轻微偏差经过联系表复核，主要动作和角色识别清楚。
- 新增六张均生成成功；两张原生中文已经目视确认，另两张用后期字、两张无字。加上此前四张原生字，全套有六张原生带字图，不会被再次叠字或伪装成可直接去字的源图。
- 完整包包含原图、按 512 像素缩放图、按单张文字设置处理的成品、配方和总览；另提供只含 76 张 512×512 成品、总览和使用说明的 QQ 包。76 张成品逐一读取尺寸确认，五页分组总览已经目视检查。生成失败记录、原图及私有配方备份均保留。
- TypeScript/Vite 生产构建与 **87/87** 全量测试通过。新增动作中一个否定式镜头短语触发了已有构图约束测试，已删除动作描述中的镜头冲突短语后再次验证通过；历史出图提示词保持真实记录。

## V7 / 0.7.0 首次引导与角色候选验证

验证日期：2026-09-09，Windows / Node 22.21.1。

- 首次成功打开显示可跳过的两步引导：已有角色图或从零描述，输入名字与可选简介后建立独立项目；也可直接继续当前项目。引导不触发图片生成，失败不清空填写内容。新增「使用指南」入口和按浏览器/站点保存的已看标记，存储拒绝、属性读取异常及 SSR 都安全回退。
- 记住当前角色项目；刷新时回到尚未完成的角色参考/母版阶段，已齐备则进入表情工坊。更换原参考图或画风取消旧母版选用，普通编辑、同一参考图和新的母版选择不误清除。旧图仍保存在历史记录中。
- 角色/母版页顶部提供上传、生成/再生成、下一步和生成记录；候选最新在前，可预览与选用，显示状态和时间。相同项目/类型正在排队或生成时禁止重复提交；下一步先保存选用结果。原有服装、人格、文字模式和 Niji 手动回导流程保留。
- 主工作目录生产构建与完整 **87/87** 测试通过。新增七项逻辑回归覆盖存储拒绝/记忆、干净新角色输入和参考/画风改变时的母版失效。
- 独立本地 UI fixture（4318 端口、独立 artifact 数据目录、纯色测试图供应商）实际验证首次显示、跳过后刷新不再弹出、重新打开、从零建独立角色且刷新保留选中项目；连续生成两张角色图、预览、选择、进入母版生成及选用，再进入表情。改选旧角色图后确认母版选用清除，两张角色图和一张母版历史仍在。没有真实云端付费调用。
- 390×844 手机尺寸实测已有图路线、新建项目、文件选择器上传测试 PNG，参考图自动选定、下一步可用且生成记录显示导入历史。手机引导和编辑界面已目视检查；默认视口恢复、测试标签页及 fixture 服务均已关闭。浏览器 error/warn 日志为空。Margaret 的真实项目未被这些测试使用。
- 源码包在全新的 `artifacts/verify-source-v7/` 离线独立安装 269 个依赖，构建成功、87/87 测试通过。包内 86 项条目无私有数据、个人图片或真实 `.env`；三个 TTF 和三个 OFL 均完整，字体校验匹配。最后只补手机标题平衡换行样式及本记录，样式修改后主生产构建再次通过。
- 发布前审查涵盖全部既有 7 个可达提交、73 个历史路径、160 个历史 blob，以及 V7 当前源码；未发现真实密钥、个人图片、SQLite 或运行数据。测试中的合成凭据 URL 已逐个确认是拒绝输入样例。用户明确授权新建私有 GitHub 仓库，源代码与运行数据分别保留。

## V6 / 0.6.0 可选角色小剧场验证

- 用户纠正参考后以两张鲸鱼截图的姿态、道具和嘴硬反差为主；补看官方樱巫女 LINE 贴图预览。观察与原创转化记录在 `V6_REFERENCE_NOTES.md`，未搬运第三方图片、台词或角色衣装。
- 新增六个单幅小处境及「角色小剧场 12」混合选集，总配方 70。小剧场中文字段可逐张编辑、关闭后保留；角色专属道具/符号可留空，只用于已请求的道具，不改人物身份、原服装或 Niji/母版规则。原 64 项及旧选集保留，独立审查曾将 768 条旧表情提示词与 V5 逐字比较，全部相等。
- 主工作目录生产构建通过；独立源码副本的全量 **80/80** 测试通过。新增覆盖严格字段类型/长度、无效更新不落库、关闭/全空场景不注入、配方往返、70 项选择与显式排队、生成及重试快照。审查发现的“专属物件重新赋予原图笼统优先级”与主动换装冲突已修正并加入回归。
- 四张实际样图通过内置 image_gen 生成并导入：偷吃露馅与罐盖僵局含原生中文，抱紧礼物用可编辑气泡字，偏心打伞无字。完整实际提示词及来源在私有 `data/samples-v6/` 和任务记录中；后续中文编辑字段翻译及提示词授权修正不回写历史清单。样图仍为白底 PNG，不冒充透明图或用户第三方图片 API 实测。
- 更新前备份项目。保留用户当时的 12 项选择，再加入六个新反应，当前选中 18 项，没有自动选择全部 70 项。此前 20 张成品与历史版本保留，当前有 24 张不同反应的最新成品。原始 Margaret PNG 的 SHA256 再次确认与 V4/V5 记录一致。
- 在新 V6 浏览器标签页验证小剧场关闭/保存、三个文本字段仍在、原有文字模式不变，再开启并保存恢复。角色专属符号与原服装依据正确显示；390×844 手机编辑区和中文摘要可读、可滚动，之后恢复默认视口；浏览器 error/warn 日志为空。未重载旧用户标签页。
- 最终 ZIP 共 74 项，包含 24 张原图、24 张 512×512 图、24 张按单张规则处理的成品、配方与联系表；每张尺寸已解析确认，四个原生文字结果保留快照，抱枕小剧场保持开启。整套联系表已目视检查新旧图的构图、中文字、道具和服装。
- 新源码包解压至 `artifacts/verify-source-v6/`，79 项条目不含个人图片、运行数据、真实 `.env`、SQLite 或预装依赖。复用 V5 的 npm 缓存离线独立安装 269 包，没有复制 `node_modules`；构建和 80/80 测试通过。源码与 `dist` 都有三字体及三 OFL 许可证，字体哈希/字节数与清单一致，锁文件一致。最终归档只补本验证记录，运行代码与已验证副本一致。

## V5 / 0.5.0 逐张排字与角色人格验证

- 单张支持无字、后期排字、随图生成，六种字样使用三款实际随包分发的中文字体与经典回退字体。三个 TTF、三个 OFL 许可证、上游固定版本及 SHA256 清单均在 `public/fonts/`；实际字形联系表已目视确认字体差异，没有用同一系统字体冒充六款字体。
- 新增可编辑的表情人格简要与三种明文预设，补充挑衅、露馅、小小得意和偷偷心软四个反应，共 64 个配方；「嘴硬小剧场 12」的 4/4/4 文字搭配属于推荐，已有用户单张设置优先。
- `npm run build` 成功，全量测试 **71/71** 通过。覆盖旧 Caption 兼容、人格和文字设置往返、生成快照与重试、原生文字防重复叠字、六种实际字体处理、换行/旋转/四种位置与 ZIP 元数据。服务商协议仍使用本地 fixture，不冒充用户第三方 API 付费联调。
- 四张新样图由内置 image_gen 使用实际项目提示词生成：两张原生中文字、一张干净源图加本地毛笔字、一张无字递蛋糕。完整提示词和来源存于私有 `data/samples-v5/` 及导入记录。后期排字位置经过成套预览调整，不改写历史生图提示词。此前 16 张原服装版本保留，当前共 20 张不同反应的成品。
- 项目在变更前备份，旧源图只改可回退的文字设置。保留原有已选 60 项并添加四项新反应，没有把用户选项强行替换为推荐套装。原始 Margaret PNG 再次校验 SHA256 与 V4 记录一致。
- 浏览器新开 V5 标签页，未重载旧用户标签页。实际验证原生字切换无字仍显示真实字并提示重生、普通源图切换随图生成显示干净原图、气泡字样采用深色字、单张毛笔字位置保存成功。角色人格简要与原服装模式正确显示。
- 390×844 手机视口已目视检查文字模式、六字体选项、位置与滑块、生成按钮，检查后恢复默认视口。浏览器 error/warn 日志为空。
- 整套导出含 20 张当前成功版本，62 项 ZIP 文件（原图、512 像素图、按单张设置处理的成品、配方与联系表）。原生文字的两张图在原图与导出中保留字，不承诺它们的 `resized/` 是无字版本；导出界面也明确提示这一点。
- 源码 ZIP 在全新的 `artifacts/verify-source-v5/` 解压，73 项条目不含私人图片、运行数据或真实 `.env`。离线缓存缺少 Vite 6.4.3，改用官方 npm registry 按锁文件独立安装 269 个包后，构建成功、71/71 测试通过；没有复制根目录依赖。构建后 `dist/fonts/` 三个 TTF 和三个许可证完整，字体哈希与清单一致。最终归档补入本验证记录与保留上游许可证空白的 Git 属性，运行代码与已验证副本一致。

## V4 / 0.4.0 原服装一致性与全身半身验证

- 根因确认：最初私有样张提示词与角色卡明确写了错误的封闭圆领毛衣，表情请求又只传母版，造成持续偏离原始开衫设计。原始`Margaret.png`未被编辑，SHA256为`6E3CBC87B18151C628DF3440F2E9E358775A3241A10067E51FD64D496120A17F`。
- 角色卡已备份后恢复原图服装描述与`outfitMode=reference`，使用新的原服装母版。新增`custom`为用户主动换装；旧配方缺字段仍默认沿用原图。
- 原图负责衣装，母版负责脸与画风。新表情任务冻结两个参考ID；OpenAI重复`image[]`/Gemini双图顺序、单图兼容、相同ID去重、任务快照、RunningHub可选第二节点与恢复不重新读取/上传图片均有测试。
- 全量测试56/56通过，TypeScript/Vite构建通过。参考图支持与请求验证使用本地fixture，未读取用户密钥或进行真实第三方付费调用。
- 12张旧样图完成服装修正版，原表情/动作/构图尽量保持；另生成4张新全身/半身样图和1张正确衣装母版。保留旧版历史，导出每个表情的最新成功版本。新图由内置image_gen生成/编辑，不冒充用户云端API验证。
- 16张最新样图ZIP已解析为50项文件，所有结果均为V4记录，`resized/`全部512×512；联系表已目视检查领口、开衫开合、肩袖、腿部服饰与配件。生成/纠正提示词保存在私有`data/samples-v4/`清单及导入任务中。
- 新增全身半身互动12（6全身＋6半身），旧56个反应和套装保留，总计60个反应。浏览器确认新样图优先显示，服装模式切换及恢复保存成功，当前保持reference。
- 独立源码包在新的`artifacts/verify-source-v4/`解压，离线安装269个依赖，生产构建与56/56测试通过；包内55项条目不含私人数据或密钥。浏览器确认RunningHub第二参考节点可展开，未提交任何凭据；error/warn日志为空。最终再次校验原始PNG的SHA256与修改前一致。

下方为前版本历史记录。

## V3 / 0.3.0 更新验证

- 最新全量测试42/42通过，TypeScript与Vite生产构建通过。新增互动方式/1–3整数张力/160字意图的保存、配方往返、非法值拒绝与长生成提示词导入保留测试。
- 重新实看2026-07-31文章中的5张鲸鱼娘JPEG静态图，具体链接、日期边界与原创演绎记录在 `V3_STATIC_RESEARCH.md`。旧塔菲资料明确保留为历史参考。
- 56张反应包含原48张及8张互动新配方；原套装ID保留，新增「可爱犯规12」。提示词分别控制互动关系、接触因果、张力和景别，普通模式不自动添加观众手。
- 四张实际新样图使用项目 `buildStickerPrompt`，通过Codex内置image_gen生成后导入：扑抱、被摸头、嘴硬捏脸、趴平发呆。图片为白底；不是用户图片API或RunningHub付费测试结果。完整提示词存在私有 `data/samples-v3/generation-manifest.json`，导入任务和导出配方也保留全长提示词。
- 原8张保留，实际样图共12张。完整ZIP已解析为38项文件；12张`resized/`均为512×512，联系表已目视检查新旧搭配、中文文字、手部归属和接触位置。
- 浏览器验证：新配方优先显示，手动勾选不重新排序；张力1/3切换、意图文字修改和保存成功。390×844手机点卡后进入编辑区，预览、镜头和互动字段正常显示。桌面预设使用单行横向卷动，保留首屏图片空间。
- 本轮全测曾遇到一项旧RunningHub取消测试的偶发竞态：它只等待远程ID而未控制查询结束时机。具体瞬时底层异常未复现；改为ID持久化后的握手与等待生成调用结束，确定性验证取消原因不被覆盖。仅测试代码变化，未以延长超时掩盖失败，也未改生产取消行为。
- 独立源码包解压到新的 `artifacts/verify-source-v3/`；离线安装269个依赖，生产构建成功，42/42测试通过。源码包53项条目不含私人data目录、密钥或运行图片；桌面与手机浏览器验证完毕，临时手机视口已恢复。

下方V2与初版记录为历史验证基线。

## V2 / 0.2.0 更新验证

- `npm test`：最新 35/35 通过；`npm run build`：TypeScript 和 Vite 通过。新增构图覆盖/旧配方兼容、动作与镜头分离、默认12张的7类构图分布，以及 RunningHub 协议与恢复测试。
- RunningHub 使用本地真实 HTTP fixture 验证上传 PNG、AI 应用/工作流节点请求、提交一次、先持久化 ID 后查询、手动/重启恢复、取消、未知提交错误码与 HTTP 408/409/5xx、不匹配的查询 ID、密钥和节点隔离。未使用用户密钥进行真实付费调用。
- 导入4张实际生成的新图：跑来全身、坐姿送花、横躺被窝、边缘探头。之前4张保留，总计8张。新图由 Codex 内置 image_gen 生成并外部导入，不冒充工坊云端/RH接口验收。
- 新图白底；完整提示词与参考职责存于私有 `data/samples-v2/generation-manifest.json`。编辑前的项目配方备份在 `data/project-before-v2.json`。
- 浏览器确认新默认「百变可爱12」显示7种构图、2张特写，样图正常显示；单张切换到全身后恢复动作构图，保存成功，API读取到 `overrides.running.compositionId=action`。
- RunningHub 连接表单的第三方地址、应用/工作流、参考图节点、额外JSON和输出索引可操作；未提交假凭据。390×844 手机表单可滚动至保存按钮。1280×720桌面收紧顶部后，首行表情卡片位于约422px处且可见。
- 用户原标签页有未保存编辑，未重载或保存该页面；另开新版标签页进行测试。
- V2源码包在新的 `artifacts/verify-source-v2/` 目录解压；离线安装269个依赖后，构建成功、35项测试通过。包内检查为49项条目，未包含私人数据目录或`.env`。
- 实际导出8张样图的新版ZIP并解析：26项文件，所有`resized/`图为512×512；已目视核对新旧两行构图、中文字和角色特征。浏览器error/warn日志为空。

以下为初版历史验证记录，仍可用于了解独立项目与导出基线。

## 已执行

- `npm test`：23 项通过。真实本地 HTTP fixture 验证 OpenAI multipart 参考图请求与 Gemini inlineData 请求；覆盖密钥不外泄、请求去重、状态不明不自动重提、重启恢复、参考图冻结/选择、删除自定义表情后的旧图导出、指定尺寸 PNG、ZIP 内容与中文加字。
- `npm run build`：TypeScript 与 Vite 生产构建通过。
- 通过真实运行的 `/api/assets`、项目保存与外部结果导入接口，导入用户提供的 Margaret 参考图及 4 张实际生成的样图。
- 浏览器桌面与 390×844 手机尺寸检查：页面、样图、选项与底部生成操作显示正常；手机点选编辑后直接进入编辑区，隐藏侧栏不进入焦点顺序。
- 浏览器保存“啊？”文案后，成功提示与有字预览正常；Niji `detail` 显示正/侧/背＋头部/服饰细节，`turnaround` 显示三视图；输入无效 Style Reference 时显示可恢复的字段错误，不崩溃。
- 未配置 API 时，点击样张生成正确打开设置并提示配置；没有创建假成功任务。
- 浏览器成品页展示 4 项真实导入记录。服务端实际导出 Margaret 四图 ZIP，解析得到 4 张原图、4 张 512×512 无字图、4 张有字图、配方与预览联系表；目视检查中文字形和配图。
- 浏览器捕获的 error/warn 日志为空。

## 出图来源

Margaret 样图由 Codex 内置 image_gen 实际生成，以用户参考图为依据，然后通过工坊的外部导入接口载入。不是配置好的第三方 API 测试结果。第一张模拟棋盘格背景候选未作为成品导入；成品为明确标记的白底 PNG。完整生成提示词保存在用户本机忽略的 `data/samples/generation-manifest.json`。

## 仍需真实环境验证

- 用户尚未填写图片供应商密钥，因此未进行用户云端图片 API 的真实付费联调；已验证两种协议的本地契约。
- 本机未提供 Docker 命令，本次没有执行容器构建。Docker/Compose 配置和中文字体安装已提供；Windows/macOS/Linux 跨系统声明属于支持目标，不能当作三平台已全部实测。
- GitHub Actions 已提供 Windows/Ubuntu 测试构建矩阵，尚未发布或触发远程运行。
- 未在 QQ 客户端发送成品，也未申请 QQ 商城上架；尺寸仅是可选聊天导出格式。
- ComfyUI 为适配器设计预留，动态 GIF 为后续阶段。

## 独立源码复现

已把源码 ZIP 解压到新的 `artifacts/verify-source/` 目录。私有目录/密钥检查为 0 项；在新目录执行 `npm ci --offline` 安装 269 个依赖，`npm run build` 成功，`npm test` 23/23 通过。React 与 Express 的模块解析路径均位于新目录自身的 node_modules。

新目录另以 4318 端口启动，首页 HTTP 200；bootstrap 返回 1 个初始项目、48 个反应、0 个资产、0 个任务、无 API Key，确认个人图片与配置没有混入源码包。验证实例已停止；用户主工坊保留在 4317 端口。
