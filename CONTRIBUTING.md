# 贡献指南

感谢帮助改善绒绒工坊。源码结构与运行方式见 [开发指南](docs/DEVELOPMENT.md)，接口字段见 [API 规范](docs/API_CONTRACT.md)。

## 提交问题

请说明操作步骤、预期结果、实际结果、系统与 Node.js 版本。接口问题还请注明协议类型和错误状态，不要粘贴 API Key、工作室口令或含密钥的日志。

## 提交改动

1. 在独立分支完成改动，先阅读涉及模块和现有测试。
2. 保持角色参考、生成记录和用户配置的兼容；新行为请补充对应的回归验证。
3. 执行 `npm test` 与 `npm run build`，并在 PR 中说明改动目的和实际验证结果。
4. 修改操作流程或 API 时，同步更新对应文档；新字体或素材须附带可再分发的许可证。

生成接口测试使用本地 fixture。不要让测试自动消耗真实云端额度；需要真实服务验证时，在自己的环境单独执行。

## 提交范围

源码、测试、文档和必要的授权文件可以提交。`data/`、`.env`、`node_modules/`、生成图片、日志和打包产物不进入仓库。截图示例请使用可公开的素材并移除敏感信息。

PR 描述应包括用户会看到的变化、相关限制以及测试命令。无需附上开发过程的完整聊天记录或临时计划。
